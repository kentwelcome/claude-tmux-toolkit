{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/nvim-open.sh",
            "timeout": 5,
            "statusMessage": "Syncing nvim sidebar..."
          }
        ]
      }
    ]
  }
}
